package com.property.propertyuser.utils

/*
import android.content.Context
import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import com.property.propertyuser.data.api.ApiHelper
import com.property.propertyuser.ui.main.home.HomeViewModel
import com.property.propertyuser.ui.startup.auth.login.LoginViewModel
import com.property.propertyuser.ui.startup.auth.otp.OtpViewModel


class ViewModelFactory(private val apiHelper: ApiHelper,
                       private val context: Context) :
    ViewModelProvider.Factory {

    override fun <T : ViewModel?> create(modelClass: Class<T>): T {
        if (modelClass.isAssignableFrom(HomeViewModel::class.java)) {
            return HomeViewModel(apiHelper,context) as T
        }
        if (modelClass.isAssignableFrom(LoginViewModel::class.java)) {
            return LoginViewModel(apiHelper,context) as T
        }
        if (modelClass.isAssignableFrom(OtpViewModel::class.java)) {
            return OtpViewModel(apiHelper,context) as T
        }
        throw IllegalArgumentException("Unknown class name")
    }

}
*/
